class DatosCanal {
  constructor({
    ideCanal = undefined,
    numId = undefined,
    ideTercero = undefined,
  }) {
    this.ideCanal = ideCanal;
    this.numId = numId;
    this.ideTercero = ideTercero;
  }
}

module.exports = DatosCanal;
